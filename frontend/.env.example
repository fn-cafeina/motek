[TEMPLATE]
# Opcional: same-origin por default (el dev server de Vite hace proxy de /api
# al backend en http://localhost:8080, ver vite.config.ts).
# Descomentá y ajustá si el backend corre en otro host:
# VITE_API_URL=http://localhost:8080
